[package]
name = "hal"
publish = false
version.workspace = true
edition.workspace = true
description = "Flint RTOS HAL trait definitions — no implementations"
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

# Must never depend on any arch or board crate
[dependencies]
