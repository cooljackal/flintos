[package]
name = "hal"
publish = false
version.workspace = true
edition.workspace = true
description = "FlintOS HAL trait definitions — no implementations"
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

# Must never depend on any arch or board crate
[dependencies]

[features]
# Test-only bus mocks (hal::testing), for the logical drivers' host unit tests.
# Pulls in std; never enabled for a target build.
test-support = []
