# SPDX-License-Identifier: Apache-2.0

[package]
name = "esp32-rmt"
publish = false
version.workspace = true
edition.workspace = true
description = "ESP32 RMT physical driver - programmable pulse generator"
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

# A peripheral driver names the chip's infrastructure -- the address map, clock
# gating, pin routing -- and nothing above it. That is what Layer 1 is allowed.
[dependencies]
soc-esp32 = { path = "../../../../soc/esp32" }
