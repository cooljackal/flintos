# SPDX-License-Identifier: Apache-2.0

[package]
name = "soc-rp2040"
publish = false
version.workspace = true
edition.workspace = true
description = "RP2040 SoC support — memory map and kernel SoC contract"
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
hal = { path = "../../hal" }

[features]
default = []
# RP2040-E16: USB needs clk_sys > 1.1 * 48 MHz, before the tick is calibrated.
usb-clock = []
