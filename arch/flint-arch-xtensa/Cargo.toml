[package]
name = "flint-arch-xtensa"
version = "0.1.0"
edition = "2021"
description = "Xtensa LX6 HAL implementation for Flint RTOS (ESP32 family)"

[dependencies]
flint-hal = { path = "../../flint-hal" }

[features]
default = []
